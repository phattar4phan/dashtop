[project]
name = "dashtop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "gpustat>=1.1.1",
    "psutil>=7.2.2",
    "uvicorn[standard]>=0.32.0",
]

[tool.uv]
dev-dependencies = []
